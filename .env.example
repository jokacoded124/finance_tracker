# Copy this file to .env locally if you want to use python-dotenv,
# OR set these as real environment variables on your host (recommended).

SECRET_KEY=replace-with-a-long-random-string
APP_USERNAME=admin
APP_PASSWORD_HASH=paste-output-of-generate_password_hash.py-here
